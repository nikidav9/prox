# IP или домен твоего сервера (обязательно)
SERVER_HOST=1.2.3.4

# Порт прокси (публичный, открой в firewall)
PORT=8080

# Порт управления / скачивания профиля
MGMT_PORT=8081

# Логин и пароль (если не задать — пароль генерируется автоматически)
PROXY_USER=user
PROXY_PASS=
